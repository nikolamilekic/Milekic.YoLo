image: Visual Studio 2019
init:
  - git config --global core.autocrlf input
build_script:
  - cmd: build.cmd -t AppVeyor
skip_tags: true
branches:
  only:
  - release
artifacts:
  - path: publish/*
nuget:
  project_feed: true

