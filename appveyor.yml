init:
  - git config --global core.autocrlf input
build_script:
  - cmd: build.cmd -t AppVeyor
test: off
version: 2.0.0.{build}
artifacts:
  - path: publish
    name: publish
nuget:
  project_feed: true

